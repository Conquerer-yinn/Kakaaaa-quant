# Kaka_Quant 前端开发计划

## 当前定位

前端第一版已经落地，当前文档的职责不再是“从零规划做什么”，而是明确：

1. 当前前端已经做到哪里。
2. 哪些设计已经确认收敛。
3. 下一阶段前端该继续补什么，不该重新做重。

前端仍然服务于同一个目标：

- 可真实演示
- 可讲述
- 可用于简历表达
- 不脱离项目真实数据链路

## 当前已经落地的内容

### 1. 页面结构

当前前端已经有 4 个页面：

- `/`
  项目首页
- `/market/history`
  历史数据页
- `/market/push`
  推送卡片页
- `/strategies`
  策略事件研究页

### 2. 历史数据页已经收敛的范围

历史页当前已经明确：

- 不再展示 `daily-basics`
- 只展示 `market-sentiment`
- 固定读取最新历史主表里的最近 20 个交易日数据
- 只展示当前最值得看的 3 个模块：
  - `总市场数据`
  - `高度观察`
  - `创业板专区`
- 不再展示“位置 / 相对中枢”等冗余列

### 3. 历史页交互已经确认

当前历史页已经实现：

- 图表与表格共用同一份真实后端数据
- 图表默认显示每个模块第一项数值列
- 点击表头里的其他数值列，可以切换当前柱状图指标
- 支持发起 `market-sentiment` 后台更新任务
- 支持轮询任务状态
- 支持请求取消任务

### 4. 推送卡片页已经确认

当前推送页已经接入三类卡片：

- 盘后复盘
- 竞价观察
- 盘中节奏

每张卡片都已经支持：

- 预览当前内容
- 刷新最新内容
- 通过 webhook 发送到飞书

当前真实联调状态：

- 三类卡片都已经完成真实 webhook 发送测试

## 当前前端设计共识

### 1. 前端不是重后台

当前前端不做：

- 通用任务平台面板
- 报表中心
- 下载中心
- 复杂筛选器
- 权限系统

原因是这些内容会明显拖慢项目节奏，也会稀释你这个项目当前最有价值的展示点。

### 2. 前端优先展示真实数据链

当前前端最重要的不是页面数量，而是：

- 页面展示的都是真实数据
- 图表和表格是联动的
- 历史页能真实触发后台任务
- 推送页能真实刷新和发送卡片

### 3. 当前最值得保留的展示重点

首页重点：

- 项目一句话定位
- 两条主线
- 已落地能力
- 页面入口

历史页重点：

- 最近 20 个交易日真实数据
- 图表与表格联动
- 后台更新任务状态

推送页重点：

- 三类卡片完整展示
- 每类卡片都能刷新与发送
- 能展示卡片稳定等级与错误信息

## 下一阶段前端建议

### 应该继续做的

1. 优化历史页的可读性和文案说明。
2. 继续优化推送页里卡片快照信息的展示层次。
3. 结合真实使用频率，判断是否需要给历史页补少量筛选能力。
4. 如果要继续提升简历展示效果，可补更完整的项目首页叙事。

### 现在不该做重的

1. 不做复杂全局状态管理。
2. 不做重组件库改造。
3. 不做任务历史平台。
4. 不做文件下载中心。
5. 不做与当前研究流程脱节的“平台感页面”。

## 当前前端最小后端依赖

前端当前真实使用的接口包括：

- `GET /dashboard/summary`
- `GET /market/history/market-sentiment`
- `POST /tasks/market-sentiment/run`
- `GET /tasks/market-sentiment/{task_id}`
- `POST /tasks/market-sentiment/{task_id}/cancel`
- `GET /market/push/cards`
- `POST /market/push/post-close/refresh`
- `POST /market/push/post-close/send`
- `POST /market/push/auction/refresh`
- `POST /market/push/auction/send`
- `POST /market/push/intraday/refresh`
- `POST /market/push/intraday/send`

## 当前阶段验收状态

前端第一版当前已经达到：

1. 本地可启动并访问 4 个页面。
2. 历史页展示真实历史数据，不是写死假数据。
3. 历史页可真实触发 `market-sentiment` 更新，并轮询状态。
4. 推送页可展示三类卡片当前内容。
5. 三类卡片都可执行刷新。
6. 三类卡片都已完成真实 webhook 发送联调。
7. 页面能展示成功 / 失败反馈、错误信息、当前说明。

## 一句话总结

前端第一版已经完成“展示壳 + 真实数据 + 任务联调 + 卡片联调”的闭环，下一阶段重点应该是继续抛光展示质量，而不是重新把它做成重后台。
