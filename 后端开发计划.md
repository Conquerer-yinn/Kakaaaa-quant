# Kaka_Quant 后端开发计划

## 一、文档定位

本文件当前不再用于讨论“后端要不要做”，而是用于说明：

1. 当前后端已经做到哪里。
2. 哪些后端边界已经确认。
3. 下一阶段后端该继续补什么，不该做太重。

本文件服务于当前阶段的统一共识：

**Kaka_Quant 的后端是轻量研究工作台后端，不是重型任务平台。**

## 二、当前后端现状

当前 `backend/` 已经落地的内容包括：

- `backend/main.py`
- `backend/api/routes.py`
- `backend/schemas/tasks.py`
- `backend/schemas/frontend.py`
- `backend/services/task_registry.py`
- `backend/services/task_runner.py`
- `backend/services/task_manager.py`
- `backend/services/frontend_data.py`
- `backend/services/push_cards.py`

当前已跑通的接口包括：

### 系统与任务

- `GET /health`
- `GET /tasks`
- `POST /tasks/daily-basics/run`
- `POST /tasks/market-sentiment/run`
- `GET /tasks/market-sentiment/{task_id}`
- `POST /tasks/market-sentiment/{task_id}/cancel`

### 前端读取接口

- `GET /dashboard/summary`
- `GET /market/history/market-sentiment`
- `GET /market/push/cards`

### 卡片刷新与发送接口

- `POST /market/push/post-close/refresh`
- `POST /market/push/post-close/send`
- `POST /market/push/auction/refresh`
- `POST /market/push/auction/send`
- `POST /market/push/intraday/refresh`
- `POST /market/push/intraday/send`

## 三、当前后端已经确认的职责

### 1. 统一任务调用入口

后端已经负责把代表任务封装成 HTTP 可调用形式。

当前代表任务包括：

- `daily-basics`
- `market-sentiment`

其中 `market-sentiment` 已额外支持：

- 后台执行
- 状态轮询
- 取消请求

### 2. 给前端提供最小稳定消费接口

当前后端已经承担：

- 首页概览接口
- 历史数据读取接口
- 卡片列表接口
- 卡片刷新接口
- 卡片发送接口

### 3. 保持轻量可维护

当前后端仍然坚持：

- 不引入数据库
- 不引入任务队列
- 不做复杂状态机
- 不做多用户权限系统
- 不为了抽象而抽象

## 四、当前已经确认的结构边界

### 1. 历史数据页只服务 `market-sentiment`

虽然 `daily-basics` 任务仍保留，但当前前端历史页已经不再单独展示它。

也就是说：

- `daily-basics` 当前更多是标准任务模板和后端示例任务
- 历史页后端当前只服务 `market-sentiment`

### 2. `market-sentiment` 采用轻量后台任务管理

这一块已经确认采用：

- 进程内 `ThreadPoolExecutor`
- 内存态任务状态
- 轮询 + 取消

当前不升级为：

- Redis 队列
- Celery
- 数据库存档任务中心

### 3. 卡片链路继续直接复用 `market/`

卡片相关接口当前统一由 `push_cards.py` 封装，但底层仍直接复用：

- `market/services/`
- `market/push_views/`
- `common/notifier/`

这符合当前项目阶段，不需要再额外抽象出一套重型消息平台层。

## 五、后端下一阶段建议

### 应该继续做的

1. 继续补更多关键代码的中文短注释。
2. 继续提高卡片字段完整度与错误提示清晰度。
3. 如果后续要接 Docker 或自动化调度，再补更明确的启动与部署约定。
4. 如果后续要开放更多历史数据展示，再评估是否继续扩展读取接口。

### 现在不该做重的

1. 不做数据库持久化任务状态。
2. 不做统一任务队列系统。
3. 不做大而全的任务平台首页。
4. 不做多用户权限系统。
5. 不做为了“框架完整”而增加的大量抽象层。

## 六、当前阶段性目标完成情况

当前后端已经达到：

1. Excel 任务可通过 API 调起。
2. `market-sentiment` 可通过后台任务方式运行。
3. 前端能通过 API 获取历史数据。
4. 前端能通过 API 获取卡片预览数据。
5. 前端能通过 API 触发卡片刷新和发送。
6. 后端仍保持轻量，没有引入数据库和队列。

## 七、一句话总结

Kaka_Quant 当前阶段的后端目标，已经从“补最小任务接口”推进到“前端可消费的轻量研究工作台后端”，下一阶段重点是继续增强可维护性和展示完整度，而不是把它做成重型平台。
