# 开发计划

## 第一阶段：先把项目骨架定稳

- [x] 明确项目存在两条主线：
  - 行情分析
  - 策略研究
- [x] 明确任务运行方式需要同时支持：
  - 单任务独立运行
  - 多任务统一调度
- [x] 明确当前阶段以 Excel 作为主要输出与复盘方式
- [x] 统一项目目录职责和任务入口规范
- [ ] 持续清理当前脚手架中不合理或过度设计的部分

## 第二阶段：跑通第一个标准任务模板

目标：将原来的“每日基础数据”脚本迁移到新结构并跑通。

- [x] 在 `market/jobs/` 下建立第一个可运行任务入口
- [x] 精简并重写 `data_engine` 中本任务需要的原始数据接口
- [x] 将“每日基础数据”的字段计算逻辑整理为可复用指标
- [x] 统一 `storage` 中的 Excel 追加、备份、路径规则
- [x] 对齐旧脚本的字段口径、列顺序、单位定义
- [x] 用一个较短日期区间完成试跑，并验证 Excel 输出是否符合预期

## 第三阶段：扩展行情分析任务

- [x] 迁移第一版市场情绪综合研究任务
- [x] 统一采用相同模式：
  - `market/jobs/` 负责运行入口
  - `data_engine/` 负责数据获取
  - `market/indicators/` 负责可复用指标
  - `storage/` 负责结果输出
- [x] 在 `market/indicators/` 中逐步沉淀口径说明和字段定义
- [x] 补充市场字段设计文档，明确字段来源、用途和状态
- [ ] 继续迁移更多旧项目指标与观察口径

## 第四阶段：建立策略研究工作流

- [x] 在 `strategies/` 目录中建立贴近代码的策略研究记录方式
- [x] 继续保留“单策略脚本反复研究”的开发方式
- [x] 将历史条件筛选结果输出到 Excel 做人工复盘
- [x] 通过 `strategies/strategy_registry.yaml` 管理哪些策略需要纳入日常运行

## 第五阶段：实现多任务统一运行

- [x] 先补最小 FastAPI 封装，允许通过 HTTP 调起现有代表任务
- [x] 提供健康检查、任务列表和两个代表任务入口
- [x] 给 `market-sentiment` 补轻量后台任务管理
- [x] 支持状态轮询与取消请求
- [ ] 在单任务流程进一步稳定后，再评估更通用的统一运行入口
- [ ] 支持多个行情任务顺序执行
- [ ] 支持多个策略任务顺序执行
- [ ] 通过配置和注册表控制启用、停用、推送等行为

## 第六阶段：扩展输出与展示

- [x] 接入第一条真实消息通知链路：盘后飞书卡片推送
- [x] 明确推送内容应采用“数据卡片”而不是直接推 Excel 文件
- [x] 打通竞价观察卡片第一版数据链路
- [x] 打通盘中节奏卡片第一版数据链路
- [x] 补最小前端展示壳
- [x] 前端历史页接入真实 `market-sentiment` 数据
- [x] 前端推送页接入三类卡片的刷新与发送接口
- [ ] 继续提升卡片字段完整度与盘中稳定性
- [ ] 评估是否需要本地 dashboard 之外的更多展示能力
- [ ] 在不阻塞当前项目推进的前提下，再评估数据库接入

## 当前新增阶段性共识

1. `market` 当前采用三层结构：原始数据层、指标计算层、输出视图层。
2. 输出视图层继续分为：
   - Excel 视图
   - 飞书卡片视图
3. 飞书卡片不应从 Excel 视图层反向取数，而应直接基于原始数据层和指标计算层生成。
4. `market` 任务分为“独立标准任务”和“综合研究任务”两类。
5. 允许综合研究任务输出同一个 Excel 的多个 sheet。
6. FastAPI 当前只做最小封装层，不做完整任务平台。
7. 前端第一版当前只把 `market-sentiment` 作为历史页展示核心，不再单独展示 `daily-basics`。
8. 历史页默认展示最近 20 个交易日，并与真实表格数据联动。

## 当前工作规则

1. 新任务必须先能独立运行，再考虑接入统一调度。
2. 业务逻辑不应直接绕过 `storage/` 去写 Excel。
3. 飞书卡片等推送逻辑不应反向依赖 Excel 视图层。
4. 策略研究进展和指标口径说明应尽量贴近对应目录维护，而不是额外分散到独立文档层。
5. 日常运行哪些策略，由配置和注册表决定，而不是写死在某个总脚本里。
6. 重要项目状态变化需要同步写入 `project_memory/`，不能只停留在聊天窗口里。
